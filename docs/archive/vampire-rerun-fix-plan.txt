Vampire rerun fix plan

1. Add an absolute baseline field.
- Add a component field for cached Vampire count baseline.
- Set it when a rerun starts from `this.session.lastVampireResults`.
- Reset it in runtime reset and progress clear paths.

2. Convert progress counts from relative to absolute.
- Keep the progress total as the full regression bank size.
- In `updateVampireProgressIndicator(...)`, add the cached baseline to `summary.item_count`.
- Do the same for the completion message helper.

3. Keep percentage math based on the full session size.
- Make sure reruns do not shrink the denominator to the current rerun subset.
- Leave `vampireProgressPercent` dividing by the full bank count.

4. Make the timing pill visible for reruns with no cached timing.
- Expand `hasTimingInfo` so it returns true during active Vampire reruns.
- Also return true when cached Vampire results exist.
- This keeps the `<app-pill>` rendered after loading JSON sessions.

5. Verify the loaded-session path.
- In `hydrateSession(...)`, restore rerun baseline state from cached Vampire results.
- Set `vampirePreserveExistingResults = true` when results exist.

6. Update tests.
- Add a rerun progress test that expects absolute counts.
- Add a visibility test for the timing pill when only cached Vampire results exist.
- Keep the existing wording and timing-detail tests.
